# Copy to keystore.properties (gitignored) or run tools/create_release_keystore.ps1
# Paths are relative to the repo root.

storeFile=signing/spatial-launcher-release.jks
storePassword=CHANGE_ME
keyAlias=spatiallauncher
keyPassword=CHANGE_ME
